#include <stdio.h>
int main(void)
{	int no;

	puts("陈煜坤=憨憨");
        puts("同意请输入1，不同意请输入2。");
	printf("输入："); scanf("%d",&no);
	if (no==1)
	     puts("好的，憨憨!");
	else
	    puts("别否认了，小学生!");

        return 0;
}

