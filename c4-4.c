#include <stdio.h>
int main(void)
{
	int i,no;
      if (no>0||no<0)
        printf("请输入一个正整数：");
        scanf("%d",&no);
        i=1;
        while(i<=no)
	   printf("%d",i++);
         printf("\n");
      else
     return 0;
}      
